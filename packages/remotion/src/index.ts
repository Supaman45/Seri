// Step 2 will wire up registerRoot(Root) and export the compositions.
// Kept as a placeholder so the workspace resolves cleanly in Step 1.

export const REMOTION_VERSION_TAG = "step-1-placeholder";
